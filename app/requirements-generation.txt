# Heavy generation dependencies — installed by install_generation.js, NOT by install.js.
# Splitting these out keeps the Phase 1 install fast and lets users browse the
# catalog / manage downloads without committing to the multi-GB MLX toolchain.

mflux>=0.10
# mflux pulls in mlx, mlx-lm, transformers, safetensors, sentencepiece, etc.
# We list a few extras directly so version conflicts are explicit:
pillow>=10.0
numpy>=1.26

# ── Diffusers engine (v1.9.0) — second local backend (PyTorch/MPS) ──────────
# For models mflux has no class for (SD3.5, Sana, Ideogram 4, …). On macOS
# arm64 the default torch wheel ships with MPS (Metal) support. protobuf +
# sentencepiece are needed by SD3-class T5 tokenizers (sentencepiece already
# comes via mflux; listed here for clarity).
torch>=2.2
diffusers>=0.31
accelerate>=0.34
protobuf>=4.0
sentencepiece>=0.2
